services:
  postgres:
    image: postgres:17
    container_name: chowidaar_db
    env_file:
      - .env.local
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

